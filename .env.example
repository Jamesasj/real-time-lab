# Example environment variables for Postgres (copy to .env and customize)
POSTGRES_USER=rtl_user
POSTGRES_PASSWORD=rtl_password
POSTGRES_DB=rtl_db
POSTGRES_PORT=5432
# PGDATA allows changing the data directory inside the container if needed
PGDATA=/var/lib/postgresql/data/pgdata

# Optional: set a non-root UID/GID to match host permissions
# PUID=1000
# PGID=1000
